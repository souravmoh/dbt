
name: 'transform'
version: '0.1'
profile: 'SampleDB'
config-version: 2

source-paths: ["Models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
data-paths: ["data"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
    - "target"
    - "dbt_modules"
    - "logs"

models:
  dbt:
      materialized: table
      staging:
        materialized: view
